namespace TicketService.Api.Models.Enums;

/// <summary>
/// The type of ticket being purchased.
/// This has been kept as an enum rather than class as this is what was given in project.
/// </summary>
public enum TicketType
{
    Adult = 0,
    Child = 1,
    Infant = 2
}
